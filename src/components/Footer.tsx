export function Footer() {
  return (
    <footer className="bg-light border-top py-3 mt-auto text-center text-muted small">
      <p style={{ marginBottom: 0 }}>ZZZ Bank © {new Date().getFullYear()}</p>
      <p className="mb-0">Calculator is for estimates only</p>
    </footer>
  );
}
